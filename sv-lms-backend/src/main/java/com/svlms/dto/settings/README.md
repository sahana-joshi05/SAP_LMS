# dto/settings

Reserved for institute-level configuration DTOs (e.g. branding, fee policy defaults,
notification preferences) if SV LMS grows to need admin-configurable settings.

Not used by the current MVP — kept here to match the standard project layout so it's
easy to slot in later without restructuring.
