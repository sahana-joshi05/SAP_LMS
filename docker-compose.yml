version: '3.8'
services:
  postgres:
    image: postgres:16
    container_name: sv-lms-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: svlms
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - sv_lms_pgdata:/var/lib/postgresql/data

volumes:
  sv_lms_pgdata:
